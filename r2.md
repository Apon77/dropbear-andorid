cd /tmp/hi
curl -LO https://dl.google.com/android/repository/android-ndk-r30-linux.zip
unzip android-ndk* 

export ANDROID_NDK_HOME=/tmp/hi/android-ndk-r30
export PATH=$PATH:$ANDROID_NDK_HOME

ndk-build --version #check it works


# Pointing to your specific path in /tmp/hi
export NDK=/tmp/hi/android-ndk-r30
export TC=$NDK/toolchains/llvm/prebuilt/linux-x86_64/bin

# Toolchain tools configurations
export CC=$TC/aarch64-linux-android24-clang
export AR=$TC/llvm-ar
export RANLIB=$TC/llvm-ranlib
export STRIP=$TC/llvm-strip

# Extract and enter Dropbear directory (make sure your dropbear archive is in this folder)
tar xf dropbear-*.tar.bz2
cd dropbear-*/

$CC --version #check compiler exits or not

